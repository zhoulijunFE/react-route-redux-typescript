import * as React from 'react';
import * as ReactDOM from 'react-dom';

export default class RegisterInfo extends React.PureComponent<{}, {}> {
    public render() {
        return <div>registerinfo</div>;
    }
}