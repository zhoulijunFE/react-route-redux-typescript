import  * as React from 'react';
import { Route } from 'react-router';

import Body from './component/Body';

const routes =
    [<Route path="first" component={Body}></Route>];

export default routes;